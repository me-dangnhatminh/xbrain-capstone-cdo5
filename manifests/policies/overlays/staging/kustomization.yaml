apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Staging: Kế thừa 100% từ base (enforcementAction: deny).
# Môi trường này phải giống hệt Production để phát hiện lỗi policy trước khi release.
